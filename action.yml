name: 'easy-webhook'
description: 'simple webhook action'
author: 'xiaoxiao'
inputs:
  webhookUrl: # change this
    required: true
    description: 'webhook url'
  webhookSecret:
    required: false
    description: 'webhook secret'
  data:
    required: true
    description: 'webhook data'
runs:
  using: 'node16'
  main: 'dist/index.js'
